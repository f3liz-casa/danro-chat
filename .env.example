PORT=3000

# Zulip bot credentials (from zuliprc)
ZULIP_REALM=https://atfedi.de
ZULIP_USERNAME=web-chat-bot@atfedi.de
ZULIP_API_KEY=

# Stream the bot will post into; topics = one per visitor session
ZULIP_STREAM=web-相談

# Optional: custom emoji prefix on visitor messages (e.g. :neocat__w:)
ZULIP_VISITOR_EMOJI=:neocat__w:

# Optional: require this siteId on Zulip widgets. If unset, no siteId check.
# Rotate by changing this value and restarting.
ZULIP_SITE_ID=
# Optional: comma-separated allowed origins (e.g. https://atfedi.de,https://www.atfedi.de)
ZULIP_ORIGINS=

# Discord adapter — set BOT token only.
# Channel/webhook are configured at runtime via /danro set-channel slash command.
DISCORD_BOT_TOKEN=
